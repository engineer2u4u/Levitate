# Shell scripts must keep LF line endings. With core.autocrlf on, Git converts
# them to CRLF on checkout, and bash then fails on the first line with
# "$'\r': command not found". The deploy script is the one that matters.
*.sh text eol=lf
